<view class="page">
  <view class="card">
    <view class="title">订阅告警通知</view>
    <input placeholder="输入业务用户ID" value="{{userId}}" bindinput="onUserIdInput" />
    <view class="tips">点击按钮将授权订阅，并绑定 userId ↔ openid。</view>
    <button type="primary" bindtap="handleSubscribe">订阅并绑定</button>
  </view>
</view>
